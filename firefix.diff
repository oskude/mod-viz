diff --git a/build/make/config-emscripten.mk b/build/make/config-emscripten.mk
index 88f905d..771024c 100644
--- a/build/make/config-emscripten.mk
+++ b/build/make/config-emscripten.mk
@@ -92,7 +92,7 @@ else ifeq ($(EMSCRIPTEN_TARGET),audioworkletprocessor)
 CPPFLAGS += -DMPT_BUILD_WASM -DMPT_BUILD_AUDIOWORKLETPROCESSOR
 CXXFLAGS += 
 CFLAGS   += 
-LDFLAGS  += -s WASM=1 -s WASM_ASYNC_COMPILATION=0 -s MODULARIZE=1 -s EXPORT_ES6=1 -s SINGLE_FILE=1
+LDFLAGS  += -s WASM=1 -s WASM_ASYNC_COMPILATION=0 -s MODULARIZE=1 -s SINGLE_FILE=1
 
 LDFLAGS += -s ALLOW_MEMORY_GROWTH=1
 
